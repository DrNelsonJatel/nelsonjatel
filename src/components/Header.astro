---
import { navItems, SITE } from '../consts';
const NAV = navItems();
const path = Astro.url.pathname;
const isActive = (href: string) => path === href || path.startsWith(href + '/');
---

<header class="site-header">
  <div class="wrap header-inner">
    <a class="wordmark" href="/">
      {SITE.wordmark}<span class="wm-desc"> · {SITE.descriptor}</span>
    </a>

    <details class="nav-disclosure">
      <summary class="nav-toggle" aria-label="Toggle menu">Menu</summary>
      <nav class="nav" aria-label="Primary">
        {NAV.map((item) => (
          <a href={item.href} class:list={['nav-link', { active: isActive(item.href) }]}>
            {item.label}
          </a>
        ))}
        <a class="btn btn--primary nav-cta" href="/speaking#book">Invite Nelson to speak</a>
      </nav>
    </details>
  </div>
</header>

<style>
  .site-header{
    position:sticky; top:0; z-index:5000;
    background:color-mix(in srgb, var(--ground-deep) 88%, transparent);
    backdrop-filter:saturate(140%) blur(8px);
    border-bottom:1px solid var(--hairline);
  }
  .header-inner{
    display:flex; align-items:center; justify-content:space-between;
    gap:1rem; min-height:64px;
  }
  .wordmark{
    font-family:var(--font-mono); font-size:0.9rem; font-weight:500;
    letter-spacing:0.04em; color:var(--ink); white-space:nowrap;
  }
  .wordmark:hover{ text-decoration:none; color:var(--ink); }
  .wm-desc{ color:var(--slate); }

  .nav{ display:flex; align-items:center; gap:clamp(1rem, 2.2vw, 2rem); }
  .nav-link{
    font-family:var(--font-mono); font-size:0.8125rem; letter-spacing:0.06em;
    text-transform:uppercase; color:var(--slate);
  }
  .nav-link:hover{ color:var(--ink); text-decoration:none; }
  .nav-link.active{ color:var(--ink); }
  .nav-cta{ font-family:var(--font-body); text-transform:none; letter-spacing:0.01em; padding:0.55em 1em; }

  /* Mobile disclosure (no JS). Desktop overrides flatten it to an inline bar. */
  .nav-toggle{
    list-style:none; cursor:pointer;
    font-family:var(--font-mono); font-size:0.8125rem; letter-spacing:0.08em;
    text-transform:uppercase; color:var(--ink);
    padding:0.4rem 0.2rem;
  }
  .nav-toggle::-webkit-details-marker{ display:none; }

  @media (max-width:767px){
    .nav-disclosure[open] .nav{
      display:flex;
      position:absolute; left:0; right:0; top:64px;
      flex-direction:column; align-items:flex-start; gap:1.1rem;
      background:var(--ground-deep); border-bottom:1px solid var(--hairline);
      padding:1.25rem var(--gutter) 1.75rem;
    }
    .nav{ display:none; }
    .nav-cta{ align-self:stretch; text-align:center; }
  }
  @media (min-width:768px){
    .nav-toggle{ display:none; }
    .nav{ display:flex !important; }
    .wm-desc{ display:inline; }
  }
  @media (max-width:420px){ .wm-desc{ display:none; } }
</style>
